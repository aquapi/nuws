##[==[
local utils = require './utils.lua'

linklib(utils.lib)
cinclude(utils.header)
]==]

require './bindings.nelua'

local uws = @record {}
return uws

-- Initialization
local app = require './app.nelua'
local uws.app: function(): *app = uws_create_app

local uws.request = require './request.nelua'
local uws.response = require './response.nelua'
