##[==[
local utils = require './utils.lua'

linklib(utils.lib)
cinclude(utils.header)
]==]

require './app.nelua'
require './bindings.nelua'
require './request.nelua'
require './response.nelua'

local uws = @record {}
return uws

-- Aliases
local uws.app = uws_app_t
local uws.create_app: function(): *uws.app = uws_create_app

local uws.request = uws_req_t
local uws.response = uws_res_t
