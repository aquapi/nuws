##[==[
  cflags '-Ofast'
]==]

local uws = require '../lib/main.nelua'

local app = uws.createApp()

app:any('/*', function(res: *uws.Response, req: *uws.Request, dat: pointer): void
  res:close('Hello')
end)

app:listen(3000)
